package com.pm.patientservice.dto.validators;

//this is used because while using valiedation annotation in the put mapping of update patient
// every time we get the error registered date required.so to manage them we created this class
public interface CreatePatientValidationGroup {
}
